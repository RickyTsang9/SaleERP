<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.business.mapper.MdSupplierMapper">
    
    <resultMap type="MdSupplier" id="MdSupplierResult">
        <result property="supplierId"    column="supplier_id"    />
        <result property="supplierCode"  column="supplier_code"  />
        <result property="supplierName"  column="supplier_name"  />
        <result property="contactPerson" column="contact_person" />
        <result property="contactPhone"  column="contact_phone"  />
        <result property="contactEmail"  column="contact_email"  />
        <result property="address"       column="address"        />
        <result property="status"        column="status"         />
        <result property="delFlag"       column="del_flag"       />
        <result property="createBy"      column="create_by"      />
        <result property="createTime"    column="create_time"    />
        <result property="updateBy"      column="update_by"      />
        <result property="updateTime"    column="update_time"    />
        <result property="remark"        column="remark"         />
    </resultMap>

    <sql id="selectMdSupplierVo">
        select supplier_id, supplier_code, supplier_name, contact_person, contact_phone, contact_email, address, status, del_flag, create_by, create_time, update_by, update_time, remark from md_supplier
    </sql>

    <select id="selectMdSupplierList" parameterType="MdSupplier" resultMap="MdSupplierResult">
        <include refid="selectMdSupplierVo"/>
        <where>  
            del_flag = '0'
            <if test="supplierCode != null  and supplierCode != ''"> and supplier_code like concat('%', #{supplierCode}, '%')</if>
            <if test="supplierName != null  and supplierName != ''"> and supplier_name like concat('%', #{supplierName}, '%')</if>
            <if test="contactPerson != null  and contactPerson != ''"> and contact_person like concat('%', #{contactPerson}, '%')</if>
            <if test="contactPhone != null  and contactPhone != ''"> and contact_phone = #{contactPhone}</if>
            <if test="status != null  and status != ''"> and status = #{status}</if>
        </where>
    </select>
    
    <select id="selectMdSupplierBySupplierId" parameterType="Long" resultMap="MdSupplierResult">
        <include refid="selectMdSupplierVo"/>
        where supplier_id = #{supplierId} and del_flag = '0'
    </select>
        
    <insert id="insertMdSupplier" parameterType="MdSupplier" useGeneratedKeys="true" keyProperty="supplierId">
        insert into md_supplier
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="supplierCode != null and supplierCode != ''">supplier_code,</if>
            <if test="supplierName != null and supplierName != ''">supplier_name,</if>
            <if test="contactPerson != null">contact_person,</if>
            <if test="contactPhone != null">contact_phone,</if>
            <if test="contactEmail != null">contact_email,</if>
            <if test="address != null">address,</if>
            <if test="status != null">status,</if>
            <if test="delFlag != null">del_flag,</if>
            <if test="createBy != null">create_by,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateBy != null">update_by,</if>
            <if test="updateTime != null">update_time,</if>
            <if test="remark != null">remark,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="supplierCode != null and supplierCode != ''">#{supplierCode},</if>
            <if test="supplierName != null and supplierName != ''">#{supplierName},</if>
            <if test="contactPerson != null">#{contactPerson},</if>
            <if test="contactPhone != null">#{contactPhone},</if>
            <if test="contactEmail != null">#{contactEmail},</if>
            <if test="address != null">#{address},</if>
            <if test="status != null">#{status},</if>
            <if test="delFlag != null">#{delFlag},</if>
            <if test="createBy != null">#{createBy},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="updateBy != null">#{updateBy},</if>
            <if test="updateTime != null">#{updateTime},</if>
            <if test="remark != null">#{remark},</if>
         </trim>
    </insert>

    <update id="updateMdSupplier" parameterType="MdSupplier">
        update md_supplier
        <trim prefix="SET" suffixOverrides=",">
            <if test="supplierCode != null and supplierCode != ''">supplier_code = #{supplierCode},</if>
            <if test="supplierName != null and supplierName != ''">supplier_name = #{supplierName},</if>
            <if test="contactPerson != null">contact_person = #{contactPerson},</if>
            <if test="contactPhone != null">contact_phone = #{contactPhone},</if>
            <if test="contactEmail != null">contact_email = #{contactEmail},</if>
            <if test="address != null">address = #{address},</if>
            <if test="status != null">status = #{status},</if>
            <if test="delFlag != null">del_flag = #{delFlag},</if>
            <if test="createBy != null">create_by = #{createBy},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="remark != null">remark = #{remark},</if>
        </trim>
        where supplier_id = #{supplierId}
    </update>

    <select id="selectSupplierReferenceCount" parameterType="Long" resultType="int">
        select
            (select count(1) from wms_purchase_order where supplier_id = #{supplierId})
            + (select count(1) from wms_purchase_return where supplier_id = #{supplierId})
            + (select count(1) from wms_inbound where supplier_id = #{supplierId})
            + (select count(1) from fin_payable where supplier_id = #{supplierId})
            + (select count(1) from fin_payment where supplier_id = #{supplierId})
    </select>

    <delete id="deleteMdSupplierBySupplierId" parameterType="Long">
        update md_supplier set del_flag = '2' where supplier_id = #{supplierId}
    </delete>

    <delete id="deleteMdSupplierBySupplierIds" parameterType="String">
        update md_supplier set del_flag = '2' where supplier_id in 
        <foreach item="supplierId" collection="array" open="(" separator="," close=")">
            #{supplierId}
        </foreach>
    </delete>
</mapper>
